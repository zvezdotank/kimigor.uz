# kimigor.uz — личная страница Ким Игоря

Мини-сайт на трёх языках: RU (`/`), EN (`/en/`), UZ (`/uz/`). У каждого языка
шесть разделов в меню — `career`, `training`, `speaking`, `jury`, `media`,
`contacts` — и страница `wunder` **вне меню**: на неё ведут только упоминания
агентства в тексте (`EXTRA_PAGES` в `build.py`).
Тёмная технологичная вёрстка: Golos Text на тексте, IBM Plex Mono на датах,
линии-волосинки вместо теней, один сигнальный синий `#6f9bff`.
Смысловой центр главной — хронология 2007–2026: лента мест работы, вехи
и столбики пройденного обучения по годам.

## Как править

Весь текст и ссылки лежат в одном файле — [`scripts/content.py`](scripts/content.py).
После правок:

```bash
python3 scripts/build.py
```

Скрипт перезаписывает `index.html`, `en/index.html`, `uz/index.html` и `sitemap.xml`.
Руками HTML-файлы не редактируем — следующая сборка затрёт правки.

Верстка — в [`site.css`](site.css). Если поменял CSS, подними `CSS_VERSION`
в [`scripts/build.py`](scripts/build.py:20), чтобы у посетителей не остался старый кэш.

## Что где

| Файл | Что это |
| --- | --- |
| `scripts/content.py` | тексты, списки, ссылки на всех трёх языках + телефон и соцсети |
| `scripts/build.py` | шаблон страницы, sitemap, JSON-LD |
| `site.css` | вся вёрстка, адаптив на 1000 и 720 px, версия для печати |
| `img/` | фотографии в WebP под размер показа; оригиналы — в `img/src/`, они не публикуются |
| `img/og-cover.jpg` | карточка 1200×630 для соцсетей, собирается вручную |
| `favicon.svg`, `favicon-32.png`, `apple-touch-icon.png` | иконки |
| `CNAME` | `kimigor.uz` для GitHub Pages |

## Правила контента

- **Каждый пункт — со ссылкой на источник.** Ссылка в поле `href`; если её нет,
  строка выводится без пометки «источник», а пустая секция не выводится совсем.
- **Секция «Отзывы» пока пустая** (`quotes: []` во всех трёх языках) — как только
  появятся реальные цитаты, добавляем их туда, и секция сама появится на странице.
- **Кейсы с цифрами временно сняты.** Кейс «+66% застройщику» убран по просьбе Игоря
  до того, как соберётся полный набор кейсов и его можно будет подать одним блоком.
- **Шкала должностей** живёт в `track` внутри каждого языка: `from` / `to` — годы,
  `key: True` подсвечивает полосу сигнальным цветом. Диапазон шкалы — `TRACK_YEARS`.
- **Facebook в контактах не выводится**, пока `FACEBOOK = ""` в `content.py`.
  Впиши туда ссылку на профиль — блок появится и в контактах, и в `sameAs` разметки.
- Числа и статусы в текстах — только те, что подтверждаются ссылками рядом.
- **Личное и агентское разделено.** «Инструменты и платформы» — стек самого Игоря,
  у раздела стоит подпись «личный стек». Статусы Google, Yandex, Meta и Telegram
  принадлежат агентству и живут на `/wunder/`, на главной их нет.

## Календарь занятости

Раздел «Занятость» на `/contacts/` рисуется скриптом [`calendar.js`](calendar.js)
по данным из Google Календаря. Пока `CAL_URL` в `scripts/content.py` пустой,
раздела на сайте нет.

Как включить:

1. [script.google.com](https://script.google.com) → новый проект, вставить
   [`gcal/apps-script.gs`](gcal/apps-script.gs) целиком.
2. Развернуть → веб-приложение, запуск от своего имени, доступ «Все».
3. Полученный `https://script.google.com/macros/s/…/exec` вписать в `CAL_URL`
   и пересобрать сайт.

Наружу уходит только время «занято с … по …»: ни названий встреч, ни участников.
События «весь день», помеченные как «свободен» и отклонённые не учитываются.
Для проверки вёрстки без реального календаря есть [`gcal/sample.json`](gcal/sample.json) —
временно поставьте `CAL_URL = "/gcal/sample.json"`.

## Темы и Content-Security-Policy

Сайт живёт в двух темах. По умолчанию берётся системная (`prefers-color-scheme`),
выбор пользователя запоминается в `localStorage` и переключается кнопкой в шапке.
Светлая тема — не инверсия тёмной: холодная бумага `#edf0f4` вместо белизны
в упор, белые панели, графитовый текст и более глубокий синий `#1f4fd8`
(исходный `#6f9bff` на светлом фоне давал 2,4:1). Обе палитры проверены
по WCAG AA во всех парах «текст на фоне».

Все цвета живут в переменных `:root`. **Захардкоженный цвет в правиле сломает
светлую тему** — добавляйте переменную.

`Content-Security-Policy` в `<meta>` включает `style-src 'self'`, а это
**запрещает атрибут `style`**. Поэтому позиции отрезков хронологии, вех
и столбиков обучения собираются в [`timeline.css`](timeline.css) при сборке.
Проверить, что инлайн-стилей не осталось:

```bash
grep -c 'style="' index.html   # должен быть 0
```

## Проверка вёрстки

Горизонтальная прокрутка — самая частая поломка: её даёт любой элемент шире
экрана, растяжка на `100vw`/`-50vw` или длинная строка без переносов. Проверять
нужно **все страницы на всех ширинах**, а не только главную на своём мониторе.

Вставьте [`scripts/check-overflow.js`](scripts/check-overflow.js) в консоль
браузера на открытом сайте: скрипт прогоняет девять страниц по десяти ширинам
от 360 до 1920 px и печатает виновников. Пусто — значит всё в порядке.

Что уже ловилось этим способом: фон плавающей шапки, растянутый на `-50vw`
(давал 562 px прокрутки на десктопе), и меню с телефоном, не помещавшиеся
в одну строку на 768 px.

## SEO

- У каждой страницы свой `canonical` и полный набор `hreflang` (ru/en/uz/x-default).
- Разметка `Person` (JSON-LD) с `sameAs` на Instagram, LinkedIn, страницу
  в правлении МАУ и профиль эксперта Kursiv.
- `robots.txt`, `sitemap.xml` с датами и страница `404.html` — в корне.
- У каждого раздела своё описание (`descrCareer`, `descrTraining` и далее
  в `content.py`) — одинаковые описания поиск считает дублями.
- Разметки `WebSite` и `BreadcrumbList` добавляются автоматически.

## Вес и скорость

Первый экран — около 70 KB: разметка, стили и портрет. Фотографии ниже
грузятся лениво, всего ~360 KB на главную.

Фотографии хранятся под размер показа (ширина 800 px, вертикальные — высота
620 px) и пережимаются в WebP. Пересобрать из оригиналов:

```python
from PIL import Image, ImageOps
im = ImageOps.exif_transpose(Image.open("img/src/имя.jpeg")).convert("RGB")
im.thumbnail((800, 620), Image.LANCZOS)
im.save("img/имя.webp", "WEBP", quality=82, method=6)
```

Имена файлов при перезаливке не меняются, поэтому к путям картинок
добавляется `?v=CSS_VERSION` — иначе у посетителей останутся старые.

## Локально посмотреть

```bash
python3 -m http.server 4327 --directory .
```

## Публикация

GitHub Pages из корня ветки `main`, репозиторий `zvezdotank/kimigor.uz`,
домен `kimigor.uz` берётся из `CNAME`.

У регистратора домена нужны A-записи GitHub Pages для `@`:
`185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
и CNAME `www` → `zvezdotank.github.io`.

Папка `concepts/` с макетами не публикуется — она в `.gitignore`.
